# No environment variables are required.
#
# The contact form uses a mailto: link — on submit it opens the visitor's own
# email client with the message pre-filled, addressed to the studio inbox
# (configured in src/lib/sendMail.ts). No backend, no API key, no third party.
